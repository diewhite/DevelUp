<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.multi.ongo.auction">
  	<select id="read" resultType="auctionDTO" parameterType="Int">
  		select auction_number,member_id,auction_category,auction_state,auction_title,hits,to_char(write_date,'yyyy-mm-dd hh24:mi:ss') as "write_date",board_content,list_photo,min_price,start_price,end_price,to_char(end_date,'yyyy-mm-dd hh24:mi:ss') as "end_date" from auction_table where auction_number = #{auction_no}
	</select>
  </mapper>
  