<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.multi.ongo.dealreq">
	
	
	<!-- 판매내역조회 : 구매요청 id 표현하기 -->
	<select id="deal_req" resultType="dealReqDTO" parameterType="Map">
		select h.deal_number, h.req_id, h.req_time		<!-- DB에서 가져와서  뿌릴 데이터  -->
		from deal_table2 d,dealhistory h		
		where d.deal_number=h.deal_number	
		and d.deal_number=#{deal_number}
	
	</select>
	
  </mapper>