<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.multi.ongo.deal">
	
	
	<!--중고거래_등록  -->
	<select id="deal_write" parameterType="dealDTO">
		insert into deal_table
		values(#{id}, #{dealType}, #{product_category}, #{product_state}, #{product_price}, #{board_title}, #{product_name}, #{board_content}, #{hits}, sysdate, deal_no.nexval)
	</select>
  </mapper>